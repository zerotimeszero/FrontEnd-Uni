const text = 'Привет пока'

const start = 2
const end = 5

console.log(text.slice(start,end))

const text2 = 'Всем привет, пока, до свидания!';
const word = "до"

console.log(text2.slice(0,text2.indexOf(word)))